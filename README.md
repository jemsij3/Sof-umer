
## Deployment Notes
If deploying to Render, ensure that the Start Command in the Render Dashboard (Settings -> Build & Deploy) is explicitly set to `node dist/server.js`. The Dashboard setting will override the `render.yaml` configuration.
